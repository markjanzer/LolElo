<div
  class="container mx-auto mt-12"
>
  <h1 class="text-center text-5xl mb-8"
    ><%= "#{@serie.league.name} #{@serie.full_name}" %>
  </h1>

  <div class="bg-core-purple-1 py-8 px-4">
    <%= content_tag :div,
      id: "chart",
      data: {
        chartData: @chart_data
      }.to_json do %>
    <% end %>
  </div>
</div>